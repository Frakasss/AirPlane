void debug_displaySomething(){
  //gb.display.setCursor(40,0);
  //gb.display.println(player.xPosition);
}


void debug_hitbox(int i){
  /*
  int ennemyRealX = ennemy[i].xPosition-player.xPosition;
  int ennemyRealY = player.yPosition-(WORLDHEIGHT-ennemy[i].yPosition)+2;  
  gb.display.setColor(RED);
  if(ennemy[i].zPosition>=ENNEMY_DISTANCE4 and ennemy[i].zPosition<ENNEMY_DISTANCE5){
    gb.display.drawRect(ennemyRealX-2,ennemyRealY-2,4,4);
  }
  
  if(ennemy[i].zPosition>=ENNEMY_DISTANCE3 and ennemy[i].zPosition<ENNEMY_DISTANCE4){
    gb.display.drawRect(ennemyRealX-3,ennemyRealY-3,6,6);
  }
  
  if(ennemy[i].zPosition>=ENNEMY_DISTANCE2 and ennemy[i].zPosition<ENNEMY_DISTANCE3){
    gb.display.drawRect(ennemyRealX-4,ennemyRealY-4,8,8);
  }
  
  if(ennemy[i].zPosition>=ENNEMY_DISTANCE1 and ennemy[i].zPosition<ENNEMY_DISTANCE2){
    gb.display.drawRect(ennemyRealX-6,ennemyRealY-6,12,12);
  }
  
  if(ennemy[i].zPosition<ENNEMY_DISTANCE1){
    gb.display.drawRect(ennemyRealX-8,ennemyRealY-8,16,16);
  }
  */
}

